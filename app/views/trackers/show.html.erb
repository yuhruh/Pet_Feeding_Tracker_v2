<p style="color: green"><%= notice %></p>

<%= render @tracker %>

<div>
  <%= link_to "Edit this tracker", edit_pet_tracker_path([@pet, @tracker]) %> |
  <%= link_to "Back to trackers", pet_trackers_path %>

  <%= button_to "Destroy this tracker", pet_tracker_path(@tracker), method: :delete %>
</div>